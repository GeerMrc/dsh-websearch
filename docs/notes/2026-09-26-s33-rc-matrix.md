# Note s33 — rc 线兼容矩阵（0.1.7-alpha.2 / 0.1.7-rc.1 / 0.1.7-rc.2）

> S33 T2 独立审核 Agent 产出（fresh context，全部 diff 重跑，不采信前序机械摘要；主上下文机械初判被驳回两项，见三问）。
> 证据源：registry.npmmirror.com tarball（registry.npmjs.org / github.com 当时不可达），17 包 × 3 版本。
> tag dsh-v0.1.7-rc.2（477b4f4205）后经 SSH 抵达；git 级 rc.1⊂rc.2 血缘已确认。tarball 级结论与 git 级一致性抽查过。

## 三行结论

| 线 | 判定 |
|---|---|
| 0.1.7-rc.1 | 插件消费面零差异（9 核心 seam + agent-loop/tools/locale/ui-settings lib/types 与 alpha.2 逐字节相同；primitives/api-remotes/app-boot/workspace-path 已分化但均加法） |
| 0.1.7-rc.2 | **零源码适配**；manifest 四处（peer +rc.1/rc.2、16 devDeps→rc.2、typert-protocol dep→rc.2、**新增补偿 devDep `@deepseek-ai/dsh-util-code-language@0.1.7-rc.2`**） |
| 横切 | rc 线 app-boot 新增插件 peer 预检（`evaluatePluginCompatibility`，includePrerelease 语义——运行时可过、安装器默认 semver 判否 ⇒ peer 枚举为安装期必需）；readonly-stack 上游 rc.2 已修 importer 站点（Reflect.set+defineProperty 自有机制），CJS-anchor 站点仍裸 |

## 三问判定

### Q1 readonly-stack —— rc.1 = NO；rc.2 = 部分修复（主上下文初判被驳回）

- rc.1 lib/index.js:1596/:1613 两处裸 `error.stack = stack.replace(...)`。
- **rc.2** `throwWithImporter` 改走新助手 `replaceErrorMessage`（lib/index.js:1598-1610）：`Reflect.set` 失败则 `Object.defineProperty` 兜底——上游以自有机制修复 importer 站点（当年观察到的 ERR_PACKAGE_PATH_NOT_EXPORTED 场景即此路径）。
- **`throwWithoutCjsAnchor`（:1631，MODULE_NOT_FOUND 的 CJS require-stack 路径）仍裸赋值**；fork 补丁 03bffa9454 的 try/catch 助手护两处。⇒ fork 残余价值 = CJS-anchor 站点守卫（现网 ESM 解析下触发面窄）。

### Q2 prepare 缺陷 —— rc 线无关（确认）

agent-loop rc.1→rc.2 仅 buildRequest/startsSeries/developer-message 变更；`TOOL_RUNTIME_SCHEDULER` prepare/dispatch 三版同行号逐字相同（lib/index.js:582-609）。0.1.6-alpha.2 缺陷属该死线独有。

### Q3 ui-primitives —— 5 消费导出在位；**新增未声明传递依赖（主上下文初判被驳回）**

- Button/Tooltip/IconGlobeOutlineRegular/IconChevronDownOutlineRegular/IconQuestionOutlineRegular 两版均在；`*Outline14` 两版均不存在（host-icons 回退继续有效）。新导出全加法；OnboardingSurface 删除（插件未用）；Button 转 ForwardRef、Tooltip +shortcutKeys/+gap（加法兼容）。
- **rc.2 lib/index.js:39 新增 `@deepseek-ai/dsh-util-code-language` 顶层导入**（rc 线新抽取包，镜像仅 0.1.7-rc.2 一版）——vitest inline primitives 依赖其可解析 ⇒ 必须补偿 devDeps。其余外部 specifier 集合不变。

## 逐 seam 矩阵（内容 diff = 除 package.json/README 外）

9 核心 seam（web/typert-protocol/settings/credentials/ui-slots/ui-renderer/agent/system-prompt/client-store）：三版 lib/types 逐字节相同（md5 抽查）；dsh-web 与 typert 的 a2→rc.1 连 README 均未动。其余：agent-loop a2→rc1=0；tools=ask 文案+displayReason 加法；api-remotes=+IncompatiblePlugin/+schedule re-export/+4 emit 事件（注入五面不变）；locale=+workspace.defaultName；ui-settings=launcher 专属 props（settings.section 契约未动）；primitives=14+50 项（CSS 大改+加法导出）；workspace-path=+fileMediaUrl（加法）；app-boot=compatibility 类型+bootstrap 变更+新宿主侧运行时依赖（插件无路径触及）。

## 适配动作清单（T3/T4 输入）

1. peer 六条 + `|| 0.1.7-rc.1 || 0.1.7-rc.2`（安装期必需 + 运行期双保险）
2. typert-protocol dep → 0.1.7-rc.2（lib 逐字节同，零风险换钉）
3. 16 devDeps → 0.1.7-rc.2；**+ `@deepseek-ai/dsh-util-code-language: 0.1.7-rc.2`**
4. 源码适配：**零**（全部消费面逐字节同或纯加法）

## 3434 必测点（rc.2 演练）

①fork 补丁树 readonly-stack 回归（装卸循环后 absence-classification 正常）②CJS-anchor 站点残留面观察 ③跨代 codec/badge（rc.2 全腿 + 015rc3 冒烟）④loader 装卸循环（新 peer 预检链路）⑤volatile settings e2e ⑥real search served-by ⑦primitives rc.2 CSS 面视觉回归（Button 圆角/Tooltip）⑧jsdom 全量（code-language 解析路径）
