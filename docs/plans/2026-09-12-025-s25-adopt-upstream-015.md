# Plan 025 — S25 采纳上游 0.1.5-rc.2：主树合并 + 插件清单化适配 + 分级上线

> 用户裁定 A（2026-09-12）+ 方法论约束：**不盲目升级**——逐项兼容性排查清单 → 逐一修复 → 实测 +
> 人工辅助验证 → 先 3423 开发测试环境 → 最后才同步生产 ~/.dsh。
> 用户三项前提：①已有 session 可弃（Session V3 风险解除，全部新起）②**配置/插件 APIKEY 必须保留复用**
> （settings.yaml + .credentials.yaml 原样保留，不重配）③生产切换 = 最后一步且需明确确认。

## 任务清单

| # | 任务 | done 条件 |
|---|---|---|
| T1 | **主树 merge-forward**：停 3423（已获指令）→ harness dev merge `dsh-v0.1.5-rc.2`（846 commits）→ 冲突解决 | merge commit 落 dev；无未解决冲突 |
| T2 | **宿主门墙**：typecheck / lint / build / test / test:gui（playbook 口径，数字亲见 exit code） | 全 exit0 或偏差清单+处置 |
| T3 | **插件适配合入**：master ← feat/s24-upgrade-015（重钉+传递依赖）→ 0.9.0 + UA×5 + CHANGELOG | 全量门墙 exit0 |
| T4 | **兼容性逐项排查清单**（实测核证，每项 file:line/亲验）：①web seam types/WebError ②credentials 四层+credentialRef ③settings 命名空间 installSection/validate/revision ④client：slots/locale/remote/renderer/settings client 面 ⑤primitives 四组件导出面 ⑥manifest inject/external 与 tsdown externals ⑦cordis.patch.yml（searchProvider/fetchProvider 钉扎 + web-search-deepseek disable 是否仍有效）⑧面板 API 重做对设置节挂载影响（浏览器亲证）⑨Session V3 对插件会话内行为（溯源徽标/工具呈现） | 清单逐项 PASS/修复，落 Note |
| T5 | **3423 切树上线**：主树重启 3423（scratch home 配置/凭据**原样保留**）+ 装 0.9.0 → 浏览器全要素 + **真实搜索一轮**（keys 在档）| 亲验证据 + 真实搜索返回 |
| T6 | **人工验证 gate**：交付验证清单呈用户辅助确认（本轮回复） | 用户确认 |
| T7 | **生产同步（仅在 T6 用户确认后另获明确指令）**：~/.dsh 保留 settings/credentials（sessions 可弃）→ anysearch 处置裁定（ADR-0013：卸 anysearch → 装 dsh-websearch）→ 3080 验证 | 用户逐项确认 |

## 红线
- T7 前零 ~/.dsh 触碰（主树合并的 symlink 效应已向用户披露且 3080 不启动，实际生效时点=用户启动时）
- 不 push；高危操作先告知
