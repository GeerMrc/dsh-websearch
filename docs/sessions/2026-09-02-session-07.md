# Session 07 — 2026-09-02

> - **所处里程碑**: M4 设置页完备（第 2 棒 = 排序+i18n 收官棒）
> - **上一棒**: Session 06 — docs/sessions/2026-09-02-session-06.md
> - **下一棒**: Session 08（roadmap ⏳ 行；本 session 收官时刷新）

## 前序 Session 审核确认（独立 Agent，新任务推进前必填）★

- **审核对象**: Session 06（网页搜索设置页骨架：client half 端到端 + 浏览器六断言，收官 master `a9a228b`）
- **审核方式**: 独立 general-purpose Agent + 骨架库 v2 四维实测——测试面：`git diff --stat 9b59d7e..a9a228b` = 27 文件 → diff 子集实跑 `pnpm vitest run tests/client/` = **27 passed/1.32s**（locales 3+controller 10+entry 6+section 8，与声称一致）+ 附加 build 亲跑（16,516B client.js + tsdown 弃用恰 ×2 复现）；静态：typecheck exit 0、lint 0w0e（38 files）；git：干净、HEAD=`a9a228b`、13 commits 逐枚吻合列表外 0 枚；交付物逐项核验（locales 15 键/inject 五面/5 卡/端口面/构建契约/899e2fd 内容 git show 坐实）+ 锚点抽验 3 处全命中（引用台账不重开成立）。**原文正本**：docs/sessions/audit-logs/2026-09-02-s07-stage0-review-of-s06.md
- **🔴 阻塞性技术债务**: 无
- **🟡 非阻塞但必须完善的债务**: 新登记 ×2（均本棒 T0 清偿）——①门墙数字三载体整段誊写（session-06:97-105 对 progress-M4:49-59/:71 三处；誊写纪律 v1.2）②STATUS 里程碑总览 M4 行漏刷（⏳ vs 位置块 🚧，dont-do 第三条同族第四次复发）
- **🟢 可同步完善的延后项**: L-2 维持 + 观察×4 归属逐条坐实（react 锚/tsdown 弃用×2/sourcemap/settingsScope）
- **结论**: PASS（可推进新任务）

## 目标

roadmap S07「优先级排序 + 覆盖标记 + i18n」：搜索链上移/下移排序写 settings（与启停同通路）；
链块渲染组合钉死覆盖标记（语义定谳 = settings 层，plan 007 D1）；i18n 门禁两脚本随棒交付
（en/zh parity 校验 + 字典外 CJK grep，注释豁免）。浏览器断言排序→settings.yaml 落盘→持久。

## 计划与审核

- 阶段 1 计划 → `docs/plans/2026-09-02-007-s07-priority-override-i18n-plan.md`（T0-T8 +
  D1-D7 + R1-R6 + 验证矩阵 + 高危预告；摸底实锚：链块 UI 落点/setEnabled 通路形态/
  deriveSnapshot/组合标量不可观测/CJK 全注释分布/locales 15 键结构/零依赖 .mjs 可行性）
- 阶段 2 独立审核轮 1 → **NEEDS REVISION**（必改 ×1：M-1 T2 会使 section.spec
  makeSnapshot 缺属性——client 面 typecheck 提交态必红，S06 🟡 失败家族；建议 ×5）→
  全数吸收 → 同 Agent 复审 **APPROVED**（7/7 闭合零残留；25 组锚点亲验全命中；D1/D6
  裁定成立不触发范围变更回路）。**原文正本**：
  docs/sessions/audit-logs/2026-09-02-s07-stage2-plan-review.md（两轮全文）
- 阶段 2.5 → AskUserQuestion 未获答，**按接力序取默认批准项自主推进**（披露：S03/S04/
  S05a/S06 同款兜底口径；本记录与 progress-M4 双落）
- 分支纪律：开发在 `feat/s07-priority-i18n`；T0 治理批直提 master（`0180c95`）；阶段 4/5
  PASS 后 `--no-ff` 合入

## 做了什么

T0 治理批，T1-T3 TDD 契约批（逐一红→绿），T4 脚本批，T5 门墙，T6 浏览器实测棒，T7 独立验证：

- T0（`0180c95`，master）：plan 007 落盘 + 阶段 0/2 audit-log 转录 + STATUS 启动刷新 +
  **🟡-1 清偿**（session-06 门墙节指针化保留事实行 + progress-M4 R5 证据格瘦身）+
  **🟡-2 清偿**（STATUS 总览 M4 行 ⏳→🚧）+ dont-do 第三条扩化（状态区清单七处收编
  STATUS 总览行——第四次复发在档）
- T1（`d7d57cd`）：locales 扩四键（moveUp/moveDown/chainDefault/chainPinned zh/en）——
  红（en.moveUp undefined，1 failed|3 passed）→ 4 passed + typecheck 双面翻转
- T2（`58e994d`）：controller `moveSearchChainEntry`（indexOf→swap→全量数组 patch，
  expectedRevision 同款）+ SectionSnapshot pinned 双派生字段——红（6 failed|10 passed）→
  client 34 passed；机械配套（M-1）makeSnapshot 补两字段，typecheck 全程绿
- T3（`c141416`）：section 搜索链 ↑/↓ 按钮（per-item aria + 边界 disabled）+
  ChainStateBadge（data-dshws-chain-state + 文案双承载）+ failed 反馈；抓取链保持只读
  （D2）——红（6 failed|7 passed）→ client 39 passed + lint 0w0e；1 失败系测试作用域
  缺陷（screen 全文档查询撞第一实例残留）修正后绿
- T4（`194c9a2`）：`scripts/check-locales.mjs`（三集合 parity，fail-loud）+
  `scripts/check-cjk.mjs`（状态机剥注释→CJK 码位扫描，字符串内容全保留）+
  package.json `check:i18n`——正例 exit 0（19 keys parity + 15 files 零 CJK 字面量）；
  拒绝路径双证（zh 独有键 exit 1 / errors.ts 注入 CJK 字面量 exit 1 行号保真）；自证时
  抓获 block 注释行号漂移缺陷并修复（10→24 对齐实物）
- T5（`f1ff487`）：门墙七命令提交态亲跑（数字见 progress-M4 门墙表，本记录按誊写纪律
  不重抄）+ Agent Note（docs/notes/2026-09-02-s07-priority-i18n.md）+ progress-M4 台账
  + settingsScope 观察闭合注记（D3：维持自持控制器）
- T6（`15bfa4d`）：浏览器 DOM 断言（主 Agent IAB 实测，evaluate 合成点击路径）——
  scratch `/tmp/dshws-s07/`（auto-scaffold + 3413 + boot `--no-open`）安装三处落盘
  （dump 352-353 标量翻转 + 547-549 insert + bundles 追加 + deps file:）→ 六断言全过
  （见下节）→ kill 84536 精确零残留
- T7：阶段 4/5 独立验证 → **PASS / COMPLETE**——R1-R6 逐条 PASS（门墙七命令亲跑零偏差
  + **node 面与 S06 tarball 解包 cmp 逐字节一致** + 双拒绝路径亲证红 + T6 实物吻合）+
  三问无阻断（契约：expectedRevision 防护成立/诚实口径成立/client 契约零破坏——package.json
  仅增 check:i18n 一行；前瞻：S08 接口就绪）；**🟡 抓获：~/.dsh/settings.yaml mtime
  21:42 落窗**（详见遗留节归属说明）→ 本记录措辞处置清偿；🟢×3 观察在档。**原文正本**：
  docs/sessions/audit-logs/2026-09-02-s07-stage45-verification.md

## 交付物

| 类型 | 路径 | 说明 |
|---|---|---|
| 代码 | `src/client/{controller.ts,section.tsx,locales.ts}` | 排序动作 + pinned 派生 + 排序 UI/徽章 + 四新键（index.ts 零变更） |
| 脚本 | `scripts/{check-locales,check-cjk}.mjs` + package.json `check:i18n` | i18n 门禁两件（零依赖 .mjs，node 22 直跑） |
| 测试 | `tests/client/{locales,controller,section}.spec.*` 扩展（+12 行为） | 基线 190→**202**（196 passed\|6 skipped） |
| 沉淀 | `docs/notes/2026-09-02-s07-priority-i18n.md` | 排序物化语义/D1 标记语义/门禁脚本实录（S09 正素材） |
| 留痕 | `docs/sessions/audit-logs/2026-09-02-s07-*.md`（3 份） | 阶段 0/2/4-5 审核输出原文逐字 |
| 实测 | `/tmp/dshws-s07/`（dump 两态 + settings-after-move.yaml + boot.log + tarball） | 六断言多源实物；/tmp 易失，关键数字已转录 |

### T6 浏览器六断言（主 Agent IAB 实测，15bfa4d message + 实物双源）

①排序 UI 呈现：10 按钮 per-item aria + 双徽章初始 default + fetch 链 0 按钮 + 边界初态精确；
②下移首项：序物化翻转（exa 首位）+ search 徽章翻 pinned + **服务端 settings.yaml 实物落盘
`dsh-websearch.searchChain` 新数组**；③reload：describe 回读序与双徽章持久；④移动后边界
复验精确跟随新序；⑤fetch 链独立（search pinned 期间保持 default + 0 按钮）；⑥kill 精确
（3413 LISTEN=0）+ 3080 监听 90269 零接触 + s05b/s06 现场 mtime 早于本棒 + tab 已关。

### 门墙实测数字

门墙数字正本 = docs/progress/progress-M4.md「门墙实测数字」表 S07 行（T5/T7 两次亲跑
一致，node v22.23.2 / pnpm 11.7.0：test 22 files 196\|6(202)、typecheck exit 0、lint
0w0e 38 files、build node 面 49.00+21.89 逐字节零漂移 + client.js 20.54 kB 新值、pack
五件、check:i18n exit 0）；本记录按誊写纪律 v1.2 不整段重抄。

## 未完成 / 遗留

- 无阻塞遗留。roadmap S07 验收全过（排序落盘实测 / parity 绿 / CJK 零命中——脚本随棒交付）
- **🟡-1 口径处置（T7 抓获，本记录清偿）**：`~/.dsh/settings.yaml` mtime 21:42:06 落于本棒
  窗口，绝对「零接触」主张不可证。归属证据链：无 dsh-websearch 键；`.credentials.yaml`
  停在 Aug 31 零变动；写入早于 T6 全部浏览器活动；21:42 前后无新 dsh/node 进程——最可能
  写入者 = 常驻 3080 实例（90269）Web UI 状态持久化（welcomeNoticeVersion，宿主
  ui-settings-general/src/index.ts:16）。**本棒主张口径修正为「T6 动作零接触」**（隔离
  法证方法学局限：mtime 核对无法归因常驻进程写入——dont-do 第四条已沉淀）
- 🟢 观察在档：vitest sourcemap / tsdown 弃用 ×2（S09 顺手）；s06 scratch home 子目录
  mtime 口径（19:56 vs 19:44，均早窗口无影响）；i18n 门禁 CI 接线缺位（本仓无 CI——
  S09 手册项）
- 🟢 新登记×2：fetch 链排序 UI / 「恢复默认序」按钮（均候选不排期，plan 007 债务映射）
- M3 with-key 用户槽位不变（本棒零接触凭据面，指引 docs/notes/2026-09-02-s05b-install-runbook.md §4）

## 下一步

1. Session 08（roadmap ⏳）：e2e 场景收口——loopback stub：断网/429/超时降级、全败
   DSHWS_CHAIN_EXHAUSTED+摘要、顺序保持、servedBy 透出、钉死直连不降级；真实 API e2e 自跳
2. S08 实测面注意：排序写通路端到端已由 T6 实物证明（settings.yaml 物化数组）——S08
   loopback 可用 settings 预置链序后断言调用时序（T7 前瞻问结论在档）

## 下一 Session 启动指令（跨窗口接力，必填）★

```
启动 Session 08 = e2e 场景收口（S07 已收官：优先级排序 + 覆盖标记 + i18n 全过——搜索链
排序写 settings + 钉死覆盖标记（settings 层语义）+ check:i18n 门禁两脚本随棒交付，基线
196 passed | 6 skipped (202)，M4 ✅；S07 观察在档：IAB evaluate 合成点击路径、~/.dsh
mtime 法证只能证「本棒动作零接触」（dont-do 第四条）、i18n CI 接线 S09 手册项；M3
with-key 槽位仍待用户回填不阻塞；GUI 实测守 user-paces 惯例 + scratch 隔离配方；当前
债务 🔴×0 🟡×0 🟢×2（fetch 排序/恢复默认按钮）+ L-2 + 观察若干；WBS/验收标准见 roadmap
⏳ 行）：
按 docs/STATUS.md 定位，依 .session-start + governance §3.1.1/§3.4 推进：计划期
（前序审核 gate 三分级 → 计划 + 独立审核 → 人工终审批准 session 计划包）→ 执行期自主
（六阶段逐一 TDD 禁批量，机械豁免除外；审核/二次确认/交叉验证独立 Agent；五禁止；高危
命令先问）→ 收尾 6 件套 + STATUS 更新 + 接力指令。
```

## 踩坑 / 经验

- **坑（TDD 测试作用域）**：同测试内二次 `render` 不卸载第一实例——`screen.queryByTestId`
  全文档查询会命中第一实例残留节点；断言「新实例无 X」必须容器内查询（T3 红→绿路上抓获）
- **坑（门禁脚本自证）**：check-cjk 首版 block 注释剥离未保留换行，命中行号漂移（报 10
  实物 24）——拒绝路径验证不仅证「能红」，还证「定位准」；修复 = 剥离时 emit 换行
- **坑（隔离法证方法学）**：`~/.dsh` mtime 核对只能证「本棒动作后未变」，不能归因常驻
  进程写入（3080 实例 welcomeNoticeVersion 持久化落窗）——隔离主张一律写「本棒动作
  零接触」并附归属证据链（dont-do 第四条）
- **经验（誊写纪律落地）**：session 记录门墙节 = 指针 + 事实主张行（「T7/T9 两次亲跑
  一致」），数字清单只住 progress 台账一处——🟡-1 清偿形态即新模板
- **经验（物化语义）**：「在默认序上移动 = 生效序物化为显式数组」一句话同时解释了
  patch 载荷形态与徽章翻转机制——范围决策（D1）写清语义后，TDD 断言自然跟随
- **经验（M-1 家族教训复用）**：给共享 interface 增必填字段时，先 grep 全部字面量构造点
  （含测试 helper）再定任务边界——S06 🟡 的失败家族在计划期就被阶段 2 审核拦下

## 当前项目状态快照

- **当前阶段**: M4 设置页完备（✅ 2026-09-02——配 key/启停 = S06 浏览器证据；排序 =
  本棒 T6 浏览器证据；热生效 = S05a 实测 + 本棒热链序回归，复合证据按 plan 007 D6 尺）。
  M3 宿主包完备（🚧 机械面 ✅；余用户 with-key 槽位回填）
- **进度**: S07 收官（排序 + 覆盖标记 + i18n 门禁）；测试基线 196\|6(202)；权威正本见
  docs/STATUS.md 当前位置块
- **阻塞项**: 无（S08 前置全就绪）

## 开发规范强化说明（固定声明，底部必填）★

- **六阶段流程合规**: 阶段 0 独立审核 S06（PASS，🟡×2 新登记 → T0 清偿）→ 阶段 1 plan
  007 落盘（D1-D7 + R1-R6 + 验证矩阵）→ 阶段 2 两轮独立审核（轮 1 NEEDS REVISION 必改
  ×1 + 建议 ×5 → 全数吸收 → 同 Agent 复审 APPROVED 7/7）→ 阶段 2.5 AskUserQuestion
  未获答 → 按接力序取默认批准项自主推进（披露，本记录与 progress-M4 双落——S03-S06
  同款兜底）→ 阶段 3 逐一执行（T0-T6 七 commit；T1-T3 TDD 红绿留痕，红证据内嵌 commit
  message；T4 拒绝路径双证；机械配套 M-1 随 T2 同 commit）→ 阶段 4/5 独立 Agent（R1-R6
  全 PASS + 门墙七命令亲跑 + node 面 cmp 逐字节取证 + 注入探针即还原 + 三问 COMPLETE；
  🟡 ~/.dsh 口径项本记录措辞处置）→ 阶段 8 本记录与原子收尾
- **独立 Agent 分离**: 阶段 0（general-purpose 四维实测）/ 阶段 2（general-purpose 两轮
  复审续用，25 锚点亲验）/ 阶段 4/5（另一 general-purpose，门墙亲跑 + cmp 取证 + 三问）；
  三份输出原文逐字落 docs/sessions/audit-logs/（无敏感值）；T6 浏览器实测为主 Agent 亲执
  （browser-use 主 Agent only 边界），无自我审核违规
- **技术债务三级处置**: 🔴 0；🟡 ×2（阶段 0 新登记）T0 清偿（`0180c95`）+ 🟡 ×1（stage45
  抓获 ~/.dsh 口径）本记录清偿（措辞修正 + dont-do 第四条沉淀）；🟢 L-2 不变 + 新登记×2
  （fetch 排序/恢复默认，plan 007 债务映射正本）+ 观察（sourcemap/tsdown/s06 mtime 口径/
  i18n CI 接线）——plan 债务映射、本记录、progress-M4 台账、commit message 四处显式声明；
  settingsScope 观察 D3 闭合注记
- **任务清单完成度**: T0-T8 共 9 项 100% 完成（T7 结论落 progress-M4 R 表 + audit-log
  正本；T8 = 本记录/STATUS/roadmap/CHANGELOG/merge/接力）
- **高危命令门控**: 计划包预告六项全在授权范围内——①/tmp/dshws-s07 scratch 自建 ②3413
  实例启停（boot 带 --no-open；kill 精确 84536，65097 ZCode Helper 客户端连接识别未误杀）
  ③scratch profile pnpm install ④浏览器自动化（凭据面零接触——settings.yaml 亲证仅
  ui-onboarding + searchChain 两键）⑤npm pack 非 publish ⑥无 push/publish/治理产物删除；
  T0 对 session-06 记录/dont-do 的治理修订已随计划包披露（🟡 清偿机制内动作）
