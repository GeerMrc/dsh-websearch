# Session 15c — web_fetch 彻底隐藏 via 官方 tools.restrict() API（终版）

> **reconstructed 补落（2026-09-10，S17 T0）**：本棒收尾时 session 记录与 plan/audit-log 均未落盘
> （S17 阶段 0 审核 🟡-2 抓获）；本记录以 CHANGELOG / STATUS 台账 / commit 区间为源重构。
> 正式正本指针：CHANGELOG「2026-09-09 — S15c」条目。

## 目标

web_fetch 对模型彻底隐藏：发现宿主官方 API `tools.restrict({deny:['web_fetch']})`（子代理系统同款机制，child-agent.ts:217 先例），配合 `agent/created` 事件对每个新 agent 自动调用 + systemPrompt 同名空段遮蔽（renderPrompt 丢弃零长度段）。

## 交付（commit `05fa639` + merge `d4db769` + 测试 `b643a9b` + 治理 `d0a4096`/`7e20156`）

- 15 行核心代码替代之前所有复杂方案（预设副本 S15a / 网关拦截 S15b 全部退役）
- 单元测试（b643a9b）：ON 每 agent deny web_fetch + prompt 遮蔽 / OFF 零调用，3 例
- 全面实测（7e20156）：标准模式新会话模型明确确认「我的可用工具里没有 web_fetch」；零报错、零引导、零改道；单元 3 例 + 浏览器 4 模式 + toggle ON/OFF 往返
- 基线 354|9(363) exit0；版本维持 0.2.2（行为对用户透明）

## 与之前方案对比（CHANGELOG 原表）

| 方案 | 工具可见？ | 报错？ | 复杂度 |
|---|---|---|---|
| 网关拦截（15b） | 可见 | 是 | 低 |
| 预设副本（15a） | 不可见 | 否 | 高 |
| **restrict()（15c 终版）** | **不可见** | **否** | **最低** |

## 遗留

- 「按 URL 取全文」能力存在缺口 → web_fetch 完整替代登记 🟡 技术债务（S16-P0 落 CHANGELOG；P1 参数对齐后评估恢复路径——Firecrawl 云端/官方回退/多工具 fetch 链）
