# Session 15b — 网关-only 接管 0.2.2（用户裁定简化）

> **reconstructed 补落（2026-09-10，S17 T0）**：本棒收尾时 session 记录与 plan/audit-log 均未落盘
> （S17 阶段 0 审核 🟡-2 抓获）；本记录以 CHANGELOG / STATUS 台账 / commit 区间为源重构。
> 正式正本指针：CHANGELOG「2026-09-09 — S15b」条目。

## 目标

web_fetch 接管收敛建只用网关（用户裁定：砍副本层，弃预设副本/默认切换/升级路径管理）。

## 交付（commit `4fddb7d` + merge `faca73d` + 治理 `b1bea38`）

- **砍副本层**：不再造预设副本、不再切默认预设、不再管理升级路径/会话恢复——整个副本层（Layer B）移除
- **只留 fetch-gate 网关**：安装=所有模式 web_fetch 拦截（调用得到「请用 web_search」指引）；关闭=正常 HTTP；卸载=零残留
- **一次性迁移**：S15a 装机自动检测旧副本 → 清除 → 恢复默认 standard
- 版本 0.2.2

## 基线与验收

详见 CHANGELOG「2026-09-09 — S15b」；**后继评价：已被 S15c 取代**（网关拦截层退役，restrict 在更前端面隐藏工具）——STATUS 台账 15b 行在案。

## 遗留

- 网关方案工具仍可见、调用仍报错 → 继续演进（S15c 起点）
