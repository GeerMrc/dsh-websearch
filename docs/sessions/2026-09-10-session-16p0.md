# Session 16-P0 — API 对齐 P0 修复级（S16 审计触发，修复级先行棒）

> **reconstructed 补落（2026-09-10，S17 T0）**：本棒收尾时 session 记录与 plan/audit-log 均未落盘
> （S17 阶段 0 审核 🟡-2 抓获）；本记录以 CHANGELOG / STATUS 台账 / commit 区间为源重构。
> 正式正本指针：CHANGELOG「2026-09-10 — P0 API 对齐修复」条目。

## 目标

API 对齐探查审计（93 项缺失）中的 P0 修复级：正确性/资金四项。

## 交付（commit `297aabc` + merge `d996d3d` + 治理 `6dfd80c`）

- **Exa highlights 参数迁移**：已废弃的 `highlightsPerUrl: 1` → 现行官方形态 `query + maxCharacters: 400`（消除 snippet 静默消失导致结果被丢）
- **Firecrawl scrape timeout 对齐**：显式 `timeout: 20000`（上游默认 60s vs 工具预算 30s——不设上限客户端 abort 后服务端继续烧 credit）
- **6 个 USER_AGENT 统一**：`dsh-websearch/0.1.0`/`0.2.0` → `0.2.2`
- **Tavily 头注释勘正**：API 默认 max_results 实为 10（非 5）
- **web_fetch 完整替代登记 🟡 技术债务**（P1 后评估恢复路径）

## 基线

全量 357|9(366) exit0 / typecheck 0 / lint 0e（**S17 阶段 0 勘正：实为 3 warnings**——takeover.spec vi 未用 + 参数未用 + index.ts WebFetchProvider 未用导入，S17 T0 清偿）/ i18n 54 keys。

## 后继

- P1 高价值参数批 = S17（roadmap ⏳ 行，本棒 6dfd80c 入册）
- S17 阶段 0 独立审核对本棒四维实测：产品面 PASS（🟡×4 全为登记/流程面）；正本 = audit-log 2026-09-10-s17-stage0-review-of-16p0.md
