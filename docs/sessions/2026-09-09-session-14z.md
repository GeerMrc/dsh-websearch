# Session 14z — 2026-09-09

> 插行棒：web_fetch 移除（预设层）+ 链路文件日志。AskUserQuestion 未获答按消息正文
> 默认推进（A + 立即做文件日志），一字可驳回披露。

## 前序 Session 审核确认 ★
- 审核对象：Session 14y（混合池治愈 + UI 微批，master 1709d47）
- 方式：插棒精简——T3 门墙亲跑 337|9(346) ⊇ 14y 333|9(342)（既有零破坏）；本棒
  事实调查（预设层 tool 注册权/seam 边界/logger 导出器缺失）即阶段 0 深审。
- 🔴/🟡：无；🟢：轨迹页 key 级可见（v2）；结论 PASS

## 做了什么
- 事实调查：web_fetch 工具注册权在 Agent 预设层（standard 等自带 tool-web
  fetch:true）；插件补丁只管 seam；链路 info 语句因宿主无 info 导出器而沉没。
- T1（a94961b）：停注 fetch 链（chain-fetch + firecrawl scrape 面）；文件日志
  sink（<dshHome>/logs/dsh-websearch.log，>1MB 轮转 .old，append 失败静默降级，
  chainLogFile 配置默认 true）；draw 级 key 追踪（成员 thunk 包装，尾 4 字符，
  插件域前缀不冒充链路行——直连测试零 [dshws-chain] 语义保持）；sink 4 例
  单测 + 拓扑翻 fetch=[]；337|9(346) exit0。
- T2：用户实例 .agent-presets/plugin-search-only/（standard 副本 fetch:false）。
- T3 实测（新预设会话）：模型工具面无 web_fetch（仅 web_search 被调）；文件日志
  完整回溯叙事实测——坏 key …123 三 draw 401→降级→firecrawl key …b9c1→
  served-by，全程 tail 可得。

## 交付物
plan 014z / src/chain-log.ts + index.ts（停注+追踪+sink）/ config chainLogFile /
用户预设覆写 / tests 4+1。基线 330→337 passed | 9 skipped (346)。

## 遗留
🟢：轨迹页 key 级可见（v2 候选）；S15 手册补「仅插件搜索」预设用法与
chainLogFile 说明。用户待办不变（anysearch 有效 key 重配）。

## 开发规范强化说明 ★
六阶段：插棒精简（事实调查=阶段0深审；用户默认裁定披露）→ T1 TDD → 门墙 →
实例实测 → 本记录原子收尾。独立面：本棒无独立复审（纯增量 sink+死代码清除+
环境预设覆写，门墙+实测覆盖；如需可下棒补）。高危面：实例重启×2 + 用户预设
目录新增（均为授权常规）；无凭据变更。
