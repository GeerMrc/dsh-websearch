# dsh-websearch Session 启动协议
#
# 使用方式：每个新 session 开头输入——
#   "按 .session-start 启动 Session N"
#
# Session 目标不需要临场想——查 docs/session-roadmap.md 表格，
# 找到下一个"⏳ 待执行"的 session，目标直接从表里复制。
#
# AI 会自动执行以下流程，无需重复说明项目背景。

===================================================================
## 进入流程（前 5 分钟必做，按治理文档 session 硬约束）
===================================================================

1. 读进度看板定位：
   读 docs/STATUS.md「当前位置块」——现在到哪、下一棒、活跃债务，再按指针下钻

1.5 读上次 session 记录：
   读 docs/sessions/ 目录下最新的 session-NN.md

2. 确认运行环境（硬约束，按项目约定）：
   node --version && pnpm --version   # 要求 node >= 22.19（宿主 dsh engines）
   本机默认 node v20——构建/测试前必须先切 nvm；所有命令在项目根执行，禁止全局安装污染。

3. 读当前进度：
   读 docs/progress/ 目录下最新的 progress-<phase>.md
   确认"待启动"任务列表

3. 读最新规范（距上次 >7 天时）：
   - AGENTS.md（贡献准则 + 不要做清单）
   - docs/dont-do.md（检查有无新增禁令）

4. 确认当前状态四问：
   - 当前阶段？
   - 进行中的任务？阻塞项？
   - 上次未完成的遗留？
   - 下一步从哪里开始？

5. 前序 Session 审核确认 gate（新任务推进前必做，governance §3.1.1）★：
   在开启新任务执行推进前，必须优先派独立 Agent（与上一 session 执行 agent
   上下文隔离，禁止自我审核）对前一个 session 的任务执行做系统性审核确认：
   - 🔴 阻塞性技术债务 → 必须先修复，才允许推进新任务（债务不清，不推进新功能）
   - 🟡 非阻塞但必须完善的债务 → 本轮新任务推进前优先处理（先债后新）
   - 🟢 可同步完善的非阻塞项 → 允许延后，但必须在计划、交付物、git 提交说明中显式声明
   审核基于代码与测试实测（文件:行号证据 + 实跑），不只信上轮记录的"已完成"声称；
   结论落盘本 session 记录"前序 Session 审核确认"节（模板已含）。

===================================================================
## 6 阶段流程规范（每个 session 必须严格遵循）★
（正式规范落位：治理文档 §3.4；本节为启动侧速查版）
===================================================================

**整个项目的开发与功能实现，要有完整的 Harness 开发思路，避免相同错误反复犯/经验沉淀，
以及规范的版本迭代，项目规范的分支管理维护等相关记录，项目记忆/文档分类规范目录管理等。**

**任务审核、二次确认、交叉验证，必须严格以独立的 Agent 执行；任务规划与执行由主 Agent 承担——
避免在同一上下文窗口任务执行带来的上下文污染，执行与审核结果的不规范而影响交付质量与结果的准确性。**

**严格遵循的流程**：

  基于任务需要制定高质量可执行的完整任务清单
       ↓
  二次审核确认任务清单具有可执行性 / 避免分析不全、片面等问题
       ↓
  【阶段 2.5 人工终审：session 计划包提交用户批准——批准即授权执行期自主推进】
       ↓
  逐一执行所有任务清单（必须 100% 高质量逐一执行）
       ↓
  回顾/审核/测试确认所有任务高质量完整执行
    （如果还有未完成或有待完善的任务，则必须继续执行
     直到所有任务清单 100% 高质量完成）
       ↓
  交叉验证确认计划任务清单完成度，无技术债务/任务执行完整
    （如果任务执行不完整，则必须追问具体哪些任务未完成，
     是否继续推进未执行完成的任务；如果有技术债务遗漏任务，
     则必须继续未完成的相关任务清单任务，
     确保计划任务高质量规范执行完成）
       ↓
  回顾/汇报总结任务执行情况，并以任务摘要形式回顾

### 六阶段映射

| 阶段 | 内容 | 执行方 | 强制级 |
|------|------|--------|--------|
| 0. 前序审核 | 独立 Agent 系统审核前一 session 交付质量 + 技术债务状态（🔴/🟡/🟢 三分级 gate，governance §3.1.1；增量采信制） | 独立 Agent ×N | 强制独立 |
| 1. 计划 | plan + ADR（如涉架构决策）落盘（任务清单 T 编号 + 验证矩阵） | 主 agent | 主 Agent |
| 2. 独立审核计划 | 审核计划可执行性 → APPROVED / NEEDS REVISION（修订后复审） | 独立 general-purpose agent | 强制独立 |
| 2.5 人工终审 | session 计划包（plan 指针 + 审核结论 + 债务实况 + 高危命令预告 + 验证矩阵概览）提交用户批准 → 批准即授权执行期自主 | **用户（人工）** | 人工 gate |
| 3. 逐一执行 | TDD 逐一执行所有任务（100% 高质量，禁止批量执行；除高危命令先问外不逐步请求确认） | 主 agent | 主 Agent |
| 4. 回顾审核 | 核对验收标准逐条；不完整继续到 100%（全量测试唯一责任点） | 独立 agent | 强制独立 |
| 5. 交叉验证 | 安全/契约/前瞻追问 + 实跑冒烟（采信阶段 4 数字）；不完整追问并补完 | 独立 agent | 强制独立 |
| 6. 汇报 | 文档同步 + 合并 main + 路线图/STATUS ✅ + 里程碑证据落 progress + 任务摘要 | 主 agent | 主 Agent |

> 强制级注：0/2/4/5=强制独立审核；2.5=用户人工批准（不可由任何 Agent 代行）；1/3/6=主 Agent。
> 弹性通道（S 级[自动触发]/Spike/机械变更豁免）见 governance §3.4.6；两段式/高危命令门控/
> 验证矩阵与增量采信见 governance §3.4.7/§3.4.8。

> 多步骤任务中的审核类步骤（如"上一 session 审核"、"计划审核"、"执行 + 审核对峙"）必须各自以
> 独立 SubAgent 推进——避免占主线任务窗口上下文，任务隔离使不同任务的执行结果更准确；
> 规划与执行由主 Agent 承担。
> 并行边界：只读探索可并行；一切写操作/任务执行必须串行逐一；审核 Agent 各自独立 spawn。

===================================================================
## 独立 Agent 分离原则 ★
===================================================================

**执行 agent ≠ 审核 agent**——禁止在同一上下文窗口同时执行与审核。

- **Why**：同一上下文窗口执行+审核会导致上下文污染，审核者倾向自我确认
  （"我写的当然对"），产生虚假审核（false-green）。
- **How**：阶段 0/2/4/5 用独立审核 agent（不同上下文窗口）。
  - 阶段0/2/4/5 的审核 agent 必须独立 spawn，不共享执行 agent 的上下文。
  - 审核 agent 基于代码实测（文件:行号证据），不只信文档声称。

===================================================================
## 技术债优先原则 ★
===================================================================

**推进新 session 前，先清理阻塞级技术债务。技术债务不清，不推进新功能。**

- 阶段 0 前序审核必须实测前置 session 的遗留项状态（债务台账 L-N 编号项）。
- 🔴 阻塞债：必须在本 session 阶段 0 清理。
- 🟡 应完善债：在本 session 内新任务开工前优先清理（先债后新）。
- 🟢 安全延后：明确归属到后续阶段，且必须在计划、交付物与 git 提交说明中显式声明
  （防隐性遗漏影响最终交付）。
- 分级处置表见治理文档 §3.1.1。

===================================================================
## 禁止行为 ★
===================================================================

**所有任务执行必须严格遵循规范的流程规范，以下行为绝对禁止：**

- ❌ **禁止虚假审核**：不基于代码实测就声称"通过"；审核必须有文件:行号证据。
- ❌ **禁止自我以为**：不基于假设写实现；必须验证或防御性处理并测试覆盖。
- ❌ **禁止跨流程任务执行**：不跳过阶段（如跳过阶段2 独立审核直接执行）。
- ❌ **禁止批量任务执行**：不一次性写完所有代码再测；TDD 逐一执行（唯一例外：机械变更豁免，见治理文档 §3.4.6）。
- ❌ **禁止未经真实审核/确认作为信息来源**：不只信汇总文档的"已完成"声称，
  必须实测代码与测试。

===================================================================
## 项目速查（不用每次重读，仅参考）
===================================================================

项目名：dsh-websearch
技术栈：TypeScript + cordis 插件协议 + vitest，宿主 deepseek-harness（dsh）官方 npm 线（版本正本=package.json 与 ops runbook，勿以本行为准）
当前阶段：M1-M6+M7 全段；进度权威=docs/STATUS.md 当前位置块（本行仅参考）

核心文档：
  docs/00-architecture.md        — 整体架构
  docs/governance-sessions.md    — 治理（含 session 硬约束 §3）
  docs/STATUS.md                 — 进度单点看板（现在到哪/下一棒，唯一权威）
  docs/session-roadmap.md        — Session 路线图（预定义目标序列）
  docs/dont-do.md                — 永久禁止行为清单
  docs/progress/                 — 阶段进度台账（含已验锚点台账）

决策记录：
  docs/decisions/（ADR 序列）当前关键决策：ADR-0001 独立目录零内核外挂 / ADR-0002 完整高可用链 /
  ADR-0003 单插件+dshws- 前缀 / ADR-0004 v1 全局优先级+中立开箱 / ADR-0005 GUI spike-first

===================================================================
## 质量纪律（每个 session 必须遵守）
===================================================================

- TDD（按适用域）：核心逻辑/算法/契约类先写测试证红强制；重构类禁证红、保全绿；文档/配置类有验证手段且回归不红
- 专用环境：node >= 22.19（nvm 切换后执行；本机默认 v20 禁裸跑）
- 禁止 git add -A：明确指定文件
- 独立 agent 执行 + 独立 agent 审核：避免上下文污染（见上方"独立 Agent 分离原则"）
- Conventional Commits：feat(scope): / fix(scope): / docs:
- 链语义必测 8 项清单（AGENTS.md 测试节）每项红→绿留痕
- 凭据零明文：key 一律 credential-ref 经 credentials 服务，禁入任何文件/快照
- 安装/端到端验证只在 scratch profile + 临时 DSH_HOME 执行，禁碰 ~/.dsh/profiles/web 与运行中的 3080 实例

===================================================================
## 退出前 Checklist（6 类信息必须全部落盘才能结束 session；载体可按项目规模合并）
===================================================================

- [ ] ① 可交付物落盘（代码/文档写入对应目录）
- [ ] ② Session 记录（docs/sessions/YYYY-MM-DD-session-NN.md，用 _templates/session-template.md，
      含**前序 Session 审核确认**节 + 底部**开发规范强化说明**节）
- [ ] ③ Progress + STATUS 更新（docs/progress/progress-<phase>.md，含债务台账与已验
      锚点台账；docs/STATUS.md 台账行 ✅ + 当前位置块刷新 + roadmap 同行 ✅——同一
      原子收尾动作完成，禁止提前预写收官状态）
- [ ] ④ 踩坑沉淀（docs/rca/（若重大）+ 更新 docs/dont-do.md）
- [ ] ⑤ CHANGELOG（CHANGELOG.md（仓库根）加本轮条目 + 诚实标注遗留项）
- [ ] ⑥ 跨 Session 接力指令（session 记录末尾输出下一 session 可复制启动指令，
      见下方"跨 Session 接力格式"）★

===================================================================
## 跨 Session 接力格式（每个 session 结束必输出；本节为截断兜底副源，格式本体以治理文档 §3.2 为准）★
===================================================================

**每轮任务执行完成、治理 6 件套落盘后，必须在回复末尾输出下一 session 的
可复制启动指令**（来源：docs/session-roadmap.md 下一个"⏳ 待执行"行，
与 docs/STATUS.md 当前位置块交叉核对），供用户直接粘贴进新窗口推进，
实现跨 session / 跨窗口任务交付。

**当期格式 = 最小自举集（约 200 字）+ 指针——五语义点不得缺（治理文档指针 /
计划期与人工终审 / 六阶段独立分离逐一 TDD / 五禁止 / 高危命令门控），禁止退化
为单行格式；WBS 与证据链不内嵌（指针纪律）**：

  启动 Session {{NN}} = {{目标一句话，≤2 行；WBS/验收标准见 roadmap ⏳ 行}}：
  按 docs/STATUS.md 定位，依 .session-start + governance §3.1.1/§3.4 推进：计划期
  （前序审核 gate 三分级 → 计划 + 独立审核 → 人工终审批准 session 计划包）→ 执行期自主
  （六阶段逐一 TDD 禁批量，机械豁免除外；审核/二次确认/交叉验证独立 Agent；五禁止；高危
  命令先问）→ 收尾 6 件套 + STATUS 更新 + 接力指令。

（与治理文档 §3.2 本体逐字一致；占位符填充后使用）

结构纪律：头行目标 ≤2 行、止于「：」；最小自举集单段到底（分号连接）；句号即
结束不追加尾行。
同步写入 session 记录"下一 Session 启动指令"节（模板已含占位）。

===================================================================
## 使用方式
===================================================================

第一步（优先）：直接粘贴上一 session 记录末尾的启动指令（推荐，最小自举集 + 指针格式）
第二步（备选）：查 docs/session-roadmap.md 找到下一个"⏳ 待执行"的 session，输入：

  按 .session-start 启动 Session {{NN}}

（AI 会自动读路线图确认目标，无需你手动指定；编号取当前下一个 ⏳ 行）

如果目标需要调整，显式说明：
  按 .session-start 启动 Session {{NN}}，重点验证 <你的侧重点>

提示：粘贴指令时若发生截断/格式丢失，以本文件的当期格式为准重新生成。
